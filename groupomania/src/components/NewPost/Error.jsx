const Error = (props) => {
    const { propsTitleInput } = props
    propsTitleInput === 1 ? console.log("yes") : console.log("no");

    return (
        console.log(propsTitleInput)
    )
}

export default Error